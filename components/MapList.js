import { Text, View } from 'react-native';
import styles from '../StyleSheet';

export default function MapList() {
  return (
    <View style={styles.container}>
      <Text>In map list</Text>
    </View>
  );
}
